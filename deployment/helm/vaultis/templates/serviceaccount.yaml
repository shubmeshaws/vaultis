{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vaultis.serviceAccountName" . }}
  labels:
    {{- include "vaultis.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.externalSecrets.roleArn }}
    eks.amazonaws.com/role-arn: {{ .Values.externalSecrets.roleArn }}
    {{- end }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
